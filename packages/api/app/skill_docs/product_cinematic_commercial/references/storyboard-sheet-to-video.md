# 故事板 → 对应秒数视频

1. 故事板是一张多格合成图，不是分镜表。
2. 读板：数清有几格（或按你与用户约定的镜次），每格对应一个 `video_input`。
3. 每镜时长写成整数秒，与该格描述一致（常规 5–12；总片 15s 时可 3×5 或 5+5+5 等；30s 见 `storyboard-15s-30s.md`）。
4. 每镜 prompt 描述该格内容，产品名与参考图一致。
5. 参考连线：故事板 + 产品图 → 该镜；用户同意后同批 `generate_node`。
6. 全部成功后系统按批次镜号合成一条成片。
